// Drawer body registrations — wired per-entity in #60.
// Each module that registers a body should be imported here (or a subfolder)
// so it runs at app boot. Individual registrations live beside the pages that
// own them.
import "./drawerBodyRegistrations";
